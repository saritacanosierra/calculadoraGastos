import React from 'react';
import Title from './Title';

const Home = () => {
  return (
    <div>
      <Title text="Bienvenido a la página principal" />
      {/* Resto del contenido del componente Home */}
    </div>
  );
};

export default Home;
